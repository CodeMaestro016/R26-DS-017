# Final Project Cleanup Report

Cleanup date: 2026-08-18

## Authorization

This cleanup used the authoritative read-only audit in:

- `docs/FINAL_PROJECT_FILE_AUDIT.md`
- `results/final_project_file_audit.json`

The machine-readable audit was validated before deletion:

- `DELETE_CANDIDATE`: 219
- `UNKNOWN_REQUIRES_REVIEW`: 0
- authorized paths only: `.pytest_cache/**`, `**/__pycache__/**/*.pyc`, and `conflict.zip`

## Removed artifacts

| Artifact | Result |
|---|---|
| `.pytest_cache/` | Present and deleted; the cache regenerated by the required pytest run was also removed afterward |
| `__pycache__/` | 20 directories present and deleted; 20 regenerated directories were removed after regression/import checks |
| Generated `.pyc` files | 213 audit-time files removed; 171 regenerated files removed after regression/import checks; final count 0 |
| `conflict.zip` | Present and deleted after confirming the live `conflict/` package, its newer `occupancy_assessor.py`, the ZIP's incomplete contents, and no non-audit references |

No additional deletion candidate was discovered or removed.

## Protected content

| Check | Result |
|---|---|
| Source files removed | 0 |
| Source files modified by cleanup | 0 |
| Model files removed or modified | 0 |
| Evidence files removed or modified | 0 |
| Test source files removed | 0 |
| Historical validators removed | 0 |
| Protected SHA-256 hashes unchanged | PASS (10/10) |

The protected hash set covered `main.py`, `run_research_demo.py`, `config.py`, `environment.py`, `intersection.sumocfg`, both runtime SUMO network/route files, the MAPPO demo policy, the selected replication checkpoint, and final demo evidence. All post-cleanup hashes exactly matched their pre-cleanup values.

The pre-existing worktree modifications to `environment.py` and `negotiation_training/environment.py` were preserved and were not changed by this cleanup.

## Regression results

| Check | Result |
|---|---|
| `python -m pytest -q` | PASS — 559 passed in 15.29s |
| Lightweight `main` and `run_research_demo` import/path resolution | PASS |
| Demo policy, selected checkpoint, scenario catalogue, and SUMO configuration resolution | PASS |
| `git diff --check` | PASS |

No MAPPO training or extended-learning experiment was started. `run_research_demo.py` was imported and its required paths were resolved without executing the demonstration or rewriting result artifacts.

## Final state

- `.pytest_cache/`: absent
- `__pycache__/`: 0 directories
- `.pyc`: 0 files
- `conflict.zip`: absent
- protected hashes: unchanged
- status: `FINAL_SAFE_REPOSITORY_CLEANUP_COMPLETE`
